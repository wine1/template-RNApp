import { StyleSheet } from "react-native"

    const styles = StyleSheet.create({
        map: {
            height:600,
            width: 600,
            backgroundColor: '#fff'
        }
    })

    export default styles